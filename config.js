// ---------- Pexels API configuration ----------
// 1. Go to https://www.pexels.com/api/ and click "Get Started" (it's free).
// 2. Copy your API key from the dashboard.
// 3. Paste it below, replacing the placeholder text.
//
// Note: since this runs entirely in the browser, this key is visible to
// anyone who views the page source once deployed. That's expected for a
// client-side demo like this — Pexels' free tier is rate-limited per key,
// not meant to protect private data.

const PEXELS_API_KEY = "YOUR_PEXELS_API_KEY_HERE";
